export const BallsSize = () =>{
    const amount = 30;
    const 
    Array.from(new Array(amount - 2)
        .slice(0,15)
        .map((_, x) => `_${x}`)
        .reduce((p, c) =>{

            return `${p} ${c}`})
    return
}
